<%= form_for @line do |f| %>
	<%= f.text_area :text, :placeholder => "type your line here", :class => "form-control" %>
	<%= f.hidden_field :previous_line_id, :value => @line.previous_line_id %>
	<%= f.hidden_field :user_id, :value => current_user.id %>
	<%= f.submit :class => "form-control" %>

<% end %>