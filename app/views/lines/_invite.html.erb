<%= form_tag("/lines/#{@lines.last.id}/send_invite", method: "post") do %>
		Invite a friend to collaborate!<br />
		<%= text_field_tag  :email, nil, {:placeholder => "friend@example.com", :class => "form-control"}  %>
		<br/>
		<%= submit_tag "Send Invite!", :class => "form-control btn btn-info" %>

<% end %>
